name: Build Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:  # 手动触发

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows (release)
        run: flutter build windows --release

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: pure_live_windows_x64
          path: build\windows\x64\runner\Release\
          compression-level: 6

  # 可选：打包成安装包（需要签名跳过）
  # package-msix:
  #   needs: build-windows
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: subosito/flutter-action@v2
  #       with:
  #         channel: stable
  #         cache: true
  #     - run: flutter pub get
  #     - name: Create MSIX
  #       run: dart run msix:create --signtool-options "/td SHA256"
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: pure_live_windows_msix
  #         path: "*.msix"
